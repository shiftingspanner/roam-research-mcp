# Roam Research MCP Server - Environment Configuration
# Copy this file to .env and fill in your values.
# NEVER commit .env files containing real credentials.

# ─── Single Graph Mode ───────────────────────────────────────────────
# Use these for connecting to a single Roam graph.
ROAM_API_TOKEN=your-api-token-here
ROAM_GRAPH_NAME=your-graph-name

# ─── Multi-Graph Mode (v2.0+) ────────────────────────────────────────
# Use ROAM_GRAPHS for connecting to multiple graphs from one server.
# Uncomment and configure these instead of the single-graph vars above.
#
# ROAM_GRAPHS='{"personal":{"token":"token-1","graph":"personal-db"},"work":{"token":"token-2","graph":"work-db","protected":true}}'
# ROAM_DEFAULT_GRAPH=personal
# ROAM_SYSTEM_WRITE_KEY=your-secret-write-key

# ─── Optional Settings ───────────────────────────────────────────────
# Tag used by roam_remember/roam_recall (default: "Memories")
# ROAM_MEMORIES_TAG=Memories

# Port for HTTP Streamable transport (default: 8088)
# HTTP_STREAM_PORT=8088

# Allowed CORS origins for HTTP transport (comma-separated)
# CORS_ORIGIN=http://localhost:5678,https://roamresearch.com
